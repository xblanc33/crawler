language: node_js
dist: trusty
node_js:
  - "8"
addons:
  chrome: stable
  apt:
    packages:
      - libnss3
      - xvfb
cache:
  directories:
    - node_modules
sudo: required
services:
  - docker



before_install:
  - docker-compose up
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
 


